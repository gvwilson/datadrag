# Plan 8

1.  Give each `show` block a default name `table-001`, `table-002`, and so on. Allow users to edit this name. Do not allow duplicate names.
2.  Use a tabbed display for showing the output of `show`. The names of the tabs are the names of the `show` blocks.
