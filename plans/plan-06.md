# Plan 6

1. "No file selected" initially appears twice in the "CSV" block. It should only appear once.

2. The connection points on the top and side of boxes are too small. Double their size.

3. Remove the white border around the connection points: they should look integral to the block.

4. Remove the "run" button from the "CSV" block and add a "Run" button to the render area's upper left corner.

5. The text box in the "Filter" block is too tall: it overlaps with the bottom of the block. Change it to be tall enough for one line of text. Make the same change to other blocks.

6. Rename "Deduplicate" to "Unique".
