{
  "name": "datadrag",
  "version": "0.1.0",
  "description": "drag-and-drop data analysis",
  "license": "MIT",
  "author": "Greg Wilson",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-data.js && node scripts/build-css.js && node scripts/bundle.js",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "esbuild": "^0.27.4",
    "serve": "^14.0.0"
  },
  "dependencies": {
    "@observablehq/plot": "^0.6.17",
    "arquero": "^8.0.3"
  }
}
